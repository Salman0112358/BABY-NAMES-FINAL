import { ReactNode } from "react";
//////////////////////////////////////////////////////
export type GridProps = {
  header: string;
  children: ReactNode;
};

export type NameElementProp = {
  id: number;
  name: string;
  sex: string;
};
